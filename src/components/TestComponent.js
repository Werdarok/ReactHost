import React from 'react'

function add(x, y) {
    return x + y;
}

function TestComponent(  ) {
    //RETURN
    return (
        <div>
            Hail Satin
        </div>
    )
}

export default TestComponent;